<div class=" bg-primary-200 dark:bg-tertiary-900 border-2 border-blue-300 py-6  rounded-3xl shadow">
  <div class="mx-auto max-w-7xl px-3 lg:px-4">
    <div class="mx-auto max-w-2xl lg:max-w-4xl">
      <div class="mt-3 space-y-6 lg:mt-8 lg:space-y-12">
        <% if component.paginate? %>
          <%= render partial: 'components/pagination' %>
        <% end %>

        <%= component.render_content %>

        <% if component.paginate? %>
          <%= render partial: 'components/pagination' %>
        <% end %>
      </div>
    </div>
  </div>
</div>
